class Solution {
    public int arraySign(int[] nums) {
        int sign = 1;
        for (int num : nums) {
            if (num == 0) return 0;     // If any number is zero → product = 0
            if (num < 0) sign = -sign;  // Flip sign for each negative number
        }
        return sign;
    }
}
