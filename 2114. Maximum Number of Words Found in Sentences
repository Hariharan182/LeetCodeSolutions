class Solution {
    public int mostWordsFound(String[] sentences) {
        int ml=0;
        for(int i=0;i<sentences.length;i++){
            if(sentences[i].split(" ").length>ml){
                ml=sentences[i].split(" ").length;
            }

        }
        return ml;
    }
}
