class Solution {
    public int maxDistinct(String s) {
        Set<Character>m=new HashSet<>();
        for(char ch:s.toCharArray()){
            m.add(ch);
        }
        return m.size();
    }
}
