class Solution {
    public boolean isPalindrome(int x) {
        int reversedNum = 0;
        int temp = x;
        while(temp > 0){
            int rem = temp % 10;
            reversedNum = reversedNum * 10 + rem;
            temp /= 10;
        }
        return (reversedNum == x);
    }
}
